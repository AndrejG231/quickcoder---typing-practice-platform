import styled from "styled-components";

interface props {}

const StatPair = styled.p<props>`
  width: 100%;
  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
`;

export default StatPair;
