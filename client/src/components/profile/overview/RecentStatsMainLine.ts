import styled from "styled-components";

const RecentStatsMainLine = styled.div`
  grid-area: line;
  width: 100%;
  height: 100%;
  background: ${({ theme }) => theme.colors.b5};
`;

export default RecentStatsMainLine;
